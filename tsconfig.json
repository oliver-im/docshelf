{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "lib": ["ESNext", "DOM", "DOM.Iterable"]
  },
  "exclude": [
    "${configDir}/build",
    "${configDir}/dist",
    "${configDir}/worktrees",
    "${configDir}/packages/obsidian"
  ]
}
